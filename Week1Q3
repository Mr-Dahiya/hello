#include<bits/stdc++.h>

using namespace std;

int main() {
    string s1;
    cout<<"Enter the string"<<endl;
    getline(cin,s1);
    int length = s1.length();
    for(int i=0; i<length ;i++) {
        s1[i]=s1[length-1-i];
    }
    cout<<"Reversed string :"<<s1<<endl;
    return 0;
}
